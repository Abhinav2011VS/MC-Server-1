# Grace Period Configuration

grace-period:
  enabled: true           # Whether the grace period system is enabled (true/false)
  duration: 10            # Duration of the grace period in seconds (default is 10 seconds) (ignored if disabled)
